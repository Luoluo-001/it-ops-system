# IT运维管理系统 配置文件模板

# 1. 基础配置
# 系统密钥，建议修改为一个随机字符串
SECRET_KEY=flzx-it-ops-system-2024-secret-key-do-not-change

# 2. 数据库配置
# 默认使用 SQLite (无需修改)
# DATABASE_URI=sqlite:///instance/it_ops.db

# 如果需要使用 MySQL，请取消下方注释并修改配置
# 格式: mysql+pymysql://用户名:密码@主机地址:端口/数据库名?charset=utf8mb4
# DATABASE_URI=mysql+pymysql://root:password@127.0.0.1:3306/it_ops_system?charset=utf8mb4

# 3. 运行配置
# 监听端口
PORT=5000
# 是否开启调试模式 (True/False)
DEBUG=False
